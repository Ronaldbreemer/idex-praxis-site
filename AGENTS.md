# AGENTS.md

Project context: Astro site in `c:\Eigen bedrijf\Websites\idex praxis\idex-praxis-site`.

## Working rules
- Prefer `npm` scripts from `package.json`.
- Avoid editing build output in `dist/` or dependencies in `node_modules/`.
- Keep changes minimal and consistent with existing project structure in `src/`, `public/`, and `astro.config.mjs`.

## Common commands
- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes
- If you add new files or assets, place them under `src/` or `public/` as appropriate.
